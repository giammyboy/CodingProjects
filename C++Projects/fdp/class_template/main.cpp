#include <iostream>
#include "complex.h"

int main() {

    Complex;
    ComplexPolar b(1, 0);
    ComplexPolar c;
    c = a + b;

    std::cout << (a + b).real() << " " << (a + b).imag() << std::endl;
    return 0;
}
